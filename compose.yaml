name: docnexus

services:
  app:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - docnexus-data:/app/data
      - ./reports:/app/reports
    restart: unless-stopped

volumes:
  docnexus-data:
