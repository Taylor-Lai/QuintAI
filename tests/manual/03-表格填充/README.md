# 表格填充验收

本目录验证表格字段映射、公式计算、多源数据融合、去重排序、模板保持和异常追溯。

## 执行方法

1. 上传 `空白模板.xlsx`、源数据和场景要求中的补充材料；
2. 使用 `用户要求.txt` 创建任务，确认所有来源和目标模板均已选择；
3. 在任务详情中检查解析、映射、计算、验证和写入节点；
4. 下载结果，并使用 Excel 或兼容软件验证文件可读性与公式重算结果；
5. 使用 `期望结果.xlsx` 核对数据、公式、格式和证据来源，但不得将其作为输入。

重点检查数值类型、日期、合并单元格、公式引用、重复行、缺失值、排序和原模板样式是否正确。
