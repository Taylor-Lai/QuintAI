# 文档编辑验收

本目录验证 Word 文档的内容修改、结构调整、格式控制、脱敏保护和复杂版式保持能力。

## 执行方法

1. 上传 `原始文档.docx`，按 `编辑要求.txt` 创建任务；
2. 在任务详情中检查处理节点、质量报告和失败提示；
3. 下载交付文件并使用 Word 或兼容软件实际打开；
4. 使用 `期望结果.docx` 核对内容、结构和版式，但不得把期望文件作为输入；
5. 重点检查页眉页脚、表格、图片、样式、分页和未要求修改的区域是否保持完整。

涉及脱敏时，还应确认原始敏感信息未出现在交付物、预览文本或日志中。
