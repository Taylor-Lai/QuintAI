# 信息提取验收

本目录验证非结构化材料的字段识别、格式归一化、冲突判断、缺失处理和证据追溯。

## 执行方法

1. 按目录编号从 `01-入门` 执行到 `05-极限`；
2. 上传场景中的 `源材料`，使用 `提取字段.txt` 作为任务要求；
3. 等待任务完成，检查字段值、置信信息、证据来源和质量提示；
4. 使用 `期望结果.json` 核对结果，但不得将该文件作为输入上传；
5. 对多版本、冲突或缺失字段，确认系统提供可解释的处理结果，且未生成缺乏来源依据的内容。

建议同时记录任务 ID、客户端、耗时、失败节点和模型配置，便于回归比较。
