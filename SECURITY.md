# 安全策略

## 报告安全问题

禁止在公开 Issue、讨论区或日志截图中披露凭据、生产数据、签名文件或可直接利用的漏洞细节。安全问题应通过仓库维护者公布的私密渠道报告，并包含以下信息：

- 受影响版本与部署方式；
- 最小复现步骤；
- 实际影响和可利用前提；
- 已采取的临时缓解措施；
- 建议修复方案（如有）。

## 生产安全基线

- 使用至少 32 个字符的随机 `SECRET_KEY`；
- 为数据库、Redis、模型供应商和外部集成使用独立凭据；
- 严格限制 `CORS_ORIGINS`，通过可信反向代理提供 HTTPS；
- 通过密钥管理服务注入敏感配置，并建立轮换机制；
- 对上传文档、模型输入输出、生成报告和备份实施访问控制与生命周期管理；
- 保留鉴权、管理操作、API 密钥和 Webhook 投递的审计记录；
- 按用户和组织校验资源所有权，不得仅依赖客户端传入的资源标识；
- 定期更新依赖、扫描镜像并验证数据库恢复流程。

## Android 安全

- 发布包必须使用受控且可恢复的稳定签名；
- 正式 API 必须使用受信任证书的 HTTPS；
- `adb reverse`、模拟器地址和明文 HTTP 仅用于本地调试；
- 不在日志、BuildConfig、资源文件或客户端代码中写入服务端密钥；
- 调试包与正式包使用不同应用 ID，避免覆盖生产安装。

## 禁止提交的内容

严禁提交 `.env`、数据库文件、备份、用户上传文档、生成报告、私钥、Android keystore、签名密码、真实访问令牌或包含用户数据的模型供应商响应。
