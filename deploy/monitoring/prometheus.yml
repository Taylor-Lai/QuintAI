global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: huiwenrongtong
    metrics_path: /api/metrics
    static_configs:
      - targets: ["app:8000"]
